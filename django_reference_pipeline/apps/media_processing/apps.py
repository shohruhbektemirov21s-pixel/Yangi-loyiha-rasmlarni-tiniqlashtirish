from django.apps import AppConfig


class MediaProcessingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.media_processing"
    label = "media_processing"
    verbose_name = "Media processing"
